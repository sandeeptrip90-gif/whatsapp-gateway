import { t as GatewayApp } from "./GatewayApp-D-Aexi-D.mjs";
//#region node_modules/.nitro/vite/services/ssr/assets/dashboard-By4xr001.js
/**
* The app is served from the Express backend at /dashboard, so it needs a route
* at that path — a router `basepath` did not survive the SPA shell hydration.
*/
var SplitComponent = GatewayApp;
//#endregion
export { SplitComponent as component };
